general {
	colors = true
	interval = 5
}

order += "ipv6"
order += "disk /"
order += "battery 0"
order += "cpu_temperature 0"
order += "load"
order += "time"

ipv6 {
	format_up = " %ip "
	format_down = " no IPv6 "
}

disk "/" {
	format = " %avail "
}

battery 0 {
	format = " %remaining "
}

cpu_temperature 0 {
	format = " %degrees °C "
}

load {
	format = " %1min "
}

time {
	format = " %Y-%m-%d %H:%M %Z "
}
